#include <stdio.h>
/**
 * main - Entry point
 * Description: alphABET
 * Return: 0 (success)
 */
int main(void)
{
	int n = 97;
	int m = 65;

	while (n <= 122)
	{
		putchar(n);
		n++;
	}
	while (c <= 90)
	{
		putchar(m);
		n++;
	}
	putchar('\n');

	return (0);
}
